{
  "name": "cucumber-stats-report",
  "version": "1.0.0",
  "description": "This program generates statistics based on cucumber reports, allowing to find unstable tests.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "stats": "node main.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "open-cli": "^8.0.0"
  }
}
